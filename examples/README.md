# Synthetic demo evidence

`demo_expected_summary.json` is a compact subset of the validated offline demo summary. The full synthetic sources are authored in `manuscriptforge/demo.py` and materialized by `manuscriptforge demo OUTPUT_DIRECTORY`. No private writing or runtime output is stored here.

See the [walkthrough](../docs/demo.md) for the corpus, decisions, and checks that produce these values.
